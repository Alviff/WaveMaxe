package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Premium Immersive Dark Theme Palette
val CyberBlack = Color(0xFF050505) // Absolute immersive flat black space backdrop
val CyberDarkSurface = Color(0xFF0A0A0A) // Clean flat dark surface
val CyberMutedSurface = Color(0xFF141414) // Selected/secondary dark surface

val CyberCyan = Color(0xFF22D3EE) // Vibrant Cyan (cyan-400 / primary accent)
val NeonPink = Color(0xFFD946EF) // Magenta/Fuchsia Pink (fuchsia-500)
val NeonGreen = Color(0xFF10B981) // Emerald Green (emerald-500)
val NeonYellow = Color(0xFFF59E0B) // Warn Yellow (amber-500)

// Semi-transparent glassy levels
val GlassCard = Color(0x14FFFFFF) // Elegant white with ~8% opacity, styled for glass surfaces
val GlassBorder = Color(0x1AFFFFFF) // Border with 10% opacity for sharp boundaries
val GlassTextBacking = Color(0x0CFFFFFF) // White with 5% opacity

// Dynamic system overrides
val White = Color(0xFFFFFFFF)
val MutedText = Color(0xFFA1A1AA) // Zinc 400 for standard muted texts
val DividerColor = Color(0x14FFFFFF) // Border separator line
val TranslucentBlack = Color(0xB3000000)
